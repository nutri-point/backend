export enum RoleRank {
  Guest = 1,
  Member = 200,
  Admin = 300,
}

export enum RoleName {
  Guest = 'Guest',
  Member = 'Member',
  Admin = 'Admin',
}

export enum RoleId {
  Guest = 1,
  Member = 2,
  Admin = 3,
}
